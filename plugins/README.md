# Plugin Rules

This directory contains local JSON rule packs used by ShadowGuard during dependency risk analysis.

The default rule pack demonstrates common supply-chain risk signals, including Git-based dependencies, direct URL dependencies, local path dependencies, and pre-release versions.

Users can add custom JSON rule files to extend ShadowGuard without changing the application source code.
